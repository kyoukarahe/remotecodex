console.error(
  "liveVerifyRemoteCodex.mjs is obsolete: Discord channel creation no longer creates Codex sessions automatically. Use the remote-codex-hosts command channel commands instead.",
);
process.exitCode = 1;
